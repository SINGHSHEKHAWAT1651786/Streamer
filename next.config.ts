import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* configuration options here, any type of configuration done here */
  /* It import the configuration of Next */
};

export default nextConfig;
